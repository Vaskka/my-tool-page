---
import ToolLayout from "../../layouts/ToolLayout.astro";
import { TextDiff } from "../../components/tools/TextDiff";

const toolMeta = {
  title: "Text Diff Checker",
  description: "Compare two texts and find differences instantly. See what was added, removed, or changed line by line.",
};
---

<ToolLayout title={toolMeta.title} description={toolMeta.description} slug="text-diff">
  <TextDiff client:load />
</ToolLayout>
